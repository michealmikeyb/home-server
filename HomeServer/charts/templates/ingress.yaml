apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: http-ingress
spec:
  ingressClassName: public
  tls:
  - hosts:
    - gpmaga.com
    secretName: quickstart-example-tls
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: homeserver
            port:
              number: 80